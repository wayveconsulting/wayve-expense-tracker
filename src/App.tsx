function App() {
  return (
    <div className="container">
      <div className="card" style={{ marginTop: 'var(--spacing-2xl)' }}>
        <h1>Wayve Expense Tracker</h1>
        <p style={{ color: 'var(--color-text-secondary)', marginTop: 'var(--spacing-sm)' }}>
          Stack is alive. Let's build something.
        </p>
      </div>
    </div>
  )
}

export default App